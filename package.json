{
    "scripts": {
        "start": "node server.js",
        "heroku-postbuild": "cd frontend && npm install && npm run build",
        "serve-static": "serve -s build",
        "cypress:run": "cd frontend && npx cypress run"
    },
    "homepage": "https://recurring-manager-app.herokuapp.com/",
    "engines": {
        "node": ">= 19.6.0",
        "npm": ">= 9.4.0"
    },
    "dependencies": {
        "express": "^4.18.2"
    },
    "devDependencies": {
        "serve": "^14.2.0"
    }
}
