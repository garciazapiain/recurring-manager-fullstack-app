export type Dashboard = {
    title: string;
  };
  
  export type productObjectType = {
    added: boolean;
    id: number;
    author: number;
    category: number;
    created_date: string;
    current_inventory: number;
    inventory_updated_date: string;
    standard_size: number;
    title: string;
    unit: string;
    use_days: string;
  };